<?xml version="1.0" encoding="utf-8"?>
<!-- Pencil: Server item 64h cornerRadius=14 -->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/item_root"
    android:layout_width="match_parent"
    android:layout_height="64dp"
    android:layout_marginBottom="8dp"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:paddingHorizontal="16dp"
    android:background="@drawable/bolt_server_item_bg"
    android:clickable="true"
    android:focusable="true">

    <!-- Flag -->
    <ImageView
        android:id="@+id/iv_item_flag"
        android:layout_width="28dp"
        android:layout_height="20dp"
        android:scaleType="fitCenter" />

    <!-- Name + Ping -->
    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical"
        android:layout_marginStart="12dp">

        <TextView
            android:id="@+id/tv_item_name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="#d0dce8"
            android:textSize="13sp"
            android:fontFamily="@font/inter"
            android:textStyle="bold"
            tools:text="США — Нью-Йорк" />

        <TextView
            android:id="@+id/tv_item_ping"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="2dp"
            android:textColor="#7a9aaa"
            android:textSize="11sp"
            android:fontFamily="@font/jetbrains_mono"
            tools:text="48 ms" />
    </LinearLayout>

    <!-- Check icon -->
    <ImageView
        android:id="@+id/iv_item_check"
        android:layout_width="16dp"
        android:layout_height="16dp"
        android:src="@drawable/ic_action_done"
        android:visibility="invisible"
        tools:visibility="visible" />

</LinearLayout>
